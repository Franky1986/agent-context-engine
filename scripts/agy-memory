#!/usr/bin/env bash
set -euo pipefail

SOURCE="${BASH_SOURCE[0]}"
while [[ -L "$SOURCE" ]]; do
  DIR="$(cd "$(dirname "$SOURCE")" && pwd)"
  SOURCE="$(readlink "$SOURCE")"
  [[ "$SOURCE" != /* ]] && SOURCE="$DIR/$SOURCE"
done
SCRIPT_DIR="$(cd "$(dirname "$SOURCE")" && pwd)"
DEFAULT_ROOT="__AGENT_MEMORY_ROOT__"

if [[ -n "${AGENT_MEMORY_ROOT:-}" ]]; then
  WORKMANAGEMENT_ROOT="$AGENT_MEMORY_ROOT"
elif [[ "$DEFAULT_ROOT" != "__AGENT_MEMORY_ROOT__" ]]; then
  WORKMANAGEMENT_ROOT="$DEFAULT_ROOT"
elif [[ -f "$SCRIPT_DIR/../SKILL.md" && "$(basename "$(dirname "$SCRIPT_DIR")")" = "agent-context-engine" && "$(basename "$(dirname "$(dirname "$SCRIPT_DIR")")")" = "skills" ]]; then
  WORKMANAGEMENT_ROOT="$(cd "$SCRIPT_DIR/../../../.." && pwd)"
elif [[ -f "$SCRIPT_DIR/../SKILL.md" ]]; then
  WORKMANAGEMENT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
else
  WORKMANAGEMENT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
fi

LAUNCH_CWD="${PWD}"

if [[ ! -d "$WORKMANAGEMENT_ROOT" ]]; then
  echo "agy-memory: missing Agent Context Engine root: $WORKMANAGEMENT_ROOT" >&2
  exit 1
fi

if [[ ! -f "$WORKMANAGEMENT_ROOT/.agents/hooks.json" ]]; then
  echo "agy-memory: missing hooks config: $WORKMANAGEMENT_ROOT/.agents/hooks.json" >&2
  exit 1
fi

LAUNCH_CWD="${PWD}"
export AGENT_MEMORY_LAUNCH_CWD="$LAUNCH_CWD"
export AGENT_MEMORY_ROOT="$WORKMANAGEMENT_ROOT"

# Antigravity loads hooks from the current working directory. Start from the
# Agent Context Engine root so the central hooks are loaded, then add the original
# launch directory as a workspace.
cd "$WORKMANAGEMENT_ROOT"
exec agy --add-dir "$LAUNCH_CWD" "$@"
